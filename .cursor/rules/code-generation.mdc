---
description: 代码生成补充规范（仅本仓特有要求；通用质量规则见 code-quality.mdc）
globs: src/**/*.py, api/**/*.py, data_provider/**/*.py, tests/**/*.py
alwaysApply: false
---

# 代码生成补充规范

通用质量规则见 [code-quality.mdc](mdc:.cursor/rules/code-quality.mdc)。
具体实现模板见 [project-patterns.mdc](mdc:.cursor/rules/project-patterns.mdc)。
本文件只列**本仓特有**的写代码要求。

## 1. 注释与文档（极简原则）
- docstring / 注释一律**英文**（与 AGENTS.md 一致）。
- **默认不写注释**。代码 + 命名能表达意图就足够。
- **可写**的 3 种情况：(a) 解释 *为什么* 这么写（trade-off / workaround / 上游怪癖）；(b) 公开函数的一行 docstring 说明用途；(c) 复杂正则 / 算法的一句概述。
- **禁止**：narrating（`# Increment counter`）、复述代码（`# Loop over items`）、临时调试遗留（`# TODO: fix later`）、装饰性分隔（`# === Section ===`）、修复说明（`# Fix #123: ...`，写到 commit message 去）。
- 修复 bug 时不要顺手在代码里"留个证据注释"，commit message + git blame 已经够了。

## 2. 复用既有基础设施
- 配置：`from src.config import get_config`。
- 日志：`logger = logging.getLogger(__name__)`。
- 数据获取：`DataFetcherManager` 或具体 fetcher，不要直接 `requests.get` 拉行情。
- 并发 / 超时：`from src._concurrency import run_with_timeout, get_executor`。
- API 信封：`from api.v1.envelope_route import EnvelopeRoute` + `from api.v1.schemas.envelope import APIResponse, ApiErrorCode`。

## 3. 依赖注入
公开服务类（`SearchService` / `NotificationService` / `Analyzer`）通过构造参数传入，禁止函数内 `SearchService()` 硬实例化。便于测试 mock。

## 4. 风格与测试
- 行宽 120；遵循 `black` + `isort` + `flake8`。
- 关键变更必须做语法或测试验证：`./test.sh syntax`，或 `.venv/bin/python -m pytest -x tests/<targeted>`，或对单文件做 `python -m py_compile`。
- 改了核心逻辑同步看 `tests/`，必要时新增 / 更新测试。

## 5. 提交前自检
- 未使用的 `import` / 变量都删了？
- `Optional[T]` / 空列表 / `None` 分支处理了？
- 没有把 plan / fix 说明留成代码注释（这些写到 commit message）？
