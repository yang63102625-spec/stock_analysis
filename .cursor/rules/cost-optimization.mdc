---
description: 成本优化与行为准则 (Cost Optimization & AI Behavior Rules)
globs: *
alwaysApply: true
---

# Cursor AI 成本优化与行为准则 (Cost Optimization & AI Behavior Rules)

为了在使用 Cursor 过程中最大化效率并节省 Token 成本，AI 助手在处理本仓库的所有请求时，必须严格遵守以下准则：

## 1. 极简输出 (Extreme Conciseness)
- **直接回答**：不要输出冗长的解释、寒暄或无关的背景知识。直接给出答案、代码修改或执行结果。
- **避免重复**：不要重复用户的问题，不要重复已经写好的代码。
- **仅输出必要内容**：如果只需要修改一行代码，不要输出整个函数或整个文件。

## 2. 精准操作 (Targeted Actions)
- **局部修改**：修改现有代码时，优先使用局部替换（如 `StrReplace`），绝对避免使用 `Write` 重写整个文件，除非是新建文件。
- **精准搜索**：在确认需要修改前，不要盲目使用 `Read` 读取整个长文件或大量无关文件。使用 `Grep` 或 `Glob` 精准定位目标代码行。
- **按需读取**：如果文件很长，使用 `Read` 的 `offset` 和 `limit` 参数只读取相关代码块。

## 3. 减少对话轮数 (Minimize Turns)
- **并行调用 (Parallel Tool Calls)**：如果需要读取多个文件、执行多个独立命令或进行多次搜索，务必在一次回复中并行调用这些工具。
- **主动推断**：基于现有上下文主动推断缺失的简单信息，减少不必要的确认提问（但涉及破坏性操作或重大架构决定时仍需询问）。

## 4. 避免过度工程 (No Over-engineering)
- **最小可行修改 (Minimal Viable Edit)**：除非用户明确要求“重构”或“全面优化”，否则只做满足当前需求的最小修改。
- **不主动添加注释**：除非代码逻辑极其复杂或用户要求，否则不要主动添加显而易见的注释。
- **不主动格式化无关代码**：只修改目标逻辑，不要顺手格式化文件中的其他无关代码，以免增加 diff 大小和 Token 消耗。

## 5. 思考过程 (Thinking Process)
- 保持内部思考过程（如果可见）简短且切中要害。
- 明确目标 -> 确定最小修改范围 -> 执行工具调用。

## 6. 优先模式速查表 (Pattern Lookup First)
- 修改本仓代码前，先查 [project-patterns.mdc](mdc:.cursor/rules/project-patterns.mdc) 是否已有现成模式（API 信封 / 并发原语 / DataFrame 校验 / mixin 拆分 / 前端拦截器 / 页面拆分 / 测试断言）。
- **直接复用现有基础设施**（`_concurrency.py` / `EnvelopeRoute` / `validate_ohlcv_dataframe` 等），不要重新设计或绕开。
- 这能显著减少探索 codebase 的轮数和 PR 反复修改成本。