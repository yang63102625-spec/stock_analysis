---
description: 全局代码质量硬规则。详细实现模式见 project-patterns.mdc
globs: src/**/*.py, api/**/*.py, data_provider/**/*.py, bot/**/*.py, tests/**/*.py, *.py
alwaysApply: true
---

# Code Quality Rules

源自全局 code review 的硬性约束。**违反任何一条都需要重新设计**。
具体怎么实现见 [project-patterns.mdc](mdc:.cursor/rules/project-patterns.mdc)。

---

## 1. 模块结构

### 文件 / 类 / 函数 行数上限
- 单 Python 文件 MUST NOT exceed **800 行**。超过则按 mixin 模式拆（见 project-patterns §5）。
- 单类 MUST NOT exceed **500 行**。
- 单函数 / 方法 MUST NOT exceed **80 行**。

### 单一职责
- 每个模块 module-level docstring 必须给出一句话职责。
- 模块名需要 "and" 才能描述 → 必须拆。
- Analyzer 模块（`analyzer.py` / `stock_analyzer.py` / `market_analyzer.py` / `enhanced_market_analyzer.py`）职责互不重叠。

### 入口文件
- 项目最多 **3 个入口文件**（CLI / API server / dev tools），每个 < 50 行，仅做 import + invoke。
- 程序化分析 API 不算入口，住在 `src/services/programmatic.py`。

### 测试位置
- 所有 `test_*.py` 必须在 `tests/` 下，禁止放项目根。

### 配置拆分
- 配置类 / 模块 > 200 行 → 按域拆（`config/data_source.py`, `config/notification.py`, ...）。

---

## 1b. Single Source of Truth

只保留每个模块的最新版本，禁止多版本共存。

- **禁止保留兼容 shim**。模块改名 / 移动 / 拆分时，必须在同一改动里更新所有调用方，删除旧文件。不要留 re-export stub。
- **禁止 legacy 重复模块**。同一公开符号 MUST 只有一个 canonical 导入路径。
- **禁止仅为保旧路径而存在的 `__init__.py` re-export**。`__init__.py` 只暴露包的公共 surface。
- **重构步骤**：移动 → `rg` 全仓更新所有调用 → `git rm` 旧文件 → 跑全量测试。
- **例外**：对外公共 API（REST endpoint / CLI flag / 环境变量 / DB schema）走标准 deprecation，不在此列。

---

## 2. DRY

- 同一逻辑出现 ≥ 2 次必须抽公共函数 / mixin / 基类。
- 数据源反爬策略（rate limit / UA 轮换 / retry）必须实现在 `BaseFetcher` 或共享 mixin，单个 fetcher 不得自己写一份。
- Formatter 逻辑集中在单一模块，平台差异（WeChat / Feishu）通过参数或策略模式处理，不允许复制 formatter 文件。

---

## 3. 异常处理

### 异常分类
项目使用统一异常层级：
- `RateLimitError` — API 限流
- `NetworkError` — 连接 / 超时 / DNS
- `DataSourceUnavailableError` — 数据源不可用或返回非法数据
- `ValidationError` — 输入或数据校验失败
- `UnknownError` — 兜底

### API 响应格式
所有 endpoint 返回统一信封 `{"code": 0, "message": "success", "data": {...}, "timestamp": "..."}`。具体怎么写见 project-patterns §1。

### 禁裸 except
- **禁止** 裸 `except:` 或 `except Exception:` 不 re-raise / 不 log。
- 总是捕获 **specific** 异常类型。`except Exception as e:` 仅在最外层（main entry）使用，且必须 log full traceback。

### 网络调用
- 所有 HTTP / 外部 API 调用 MUST 设置显式 `timeout=` 参数。
- 所有外部 API 调用 MUST 有指数退避 retry。
- 关键外部 API 必须有 circuit breaker 保护（连续失败 fail-fast）。

---

## 4. 资源管理

- `ThreadPoolExecutor` 必须 module-level 或 class-level 复用，**绝不**在函数内新建。具体见 project-patterns §3。
- 数据库连接必须用连接池，不允许每次查询新建连接。
- 所有外部资源（文件 / 网络 / 线程池 / DB）必须用 `with` 或显式 `close()` / `shutdown()` 管理生命周期。

---

## 5. 缓存

- 缓存必须经统一接口（`CachingManager`）管理，禁止模块级 `_cache = {}`。
- 每个缓存项 MUST 有显式 TTL。
- 实时金融数据 TTL 必须动态：交易时段短（5-30s），盘后长（5-60min）。

---

## 6. 线程安全

### 单例
单例必须用 double-check locking + `threading.Lock`：

```python
class Singleton:
    _instance = None
    _lock = threading.Lock()

    @classmethod
    def get_instance(cls):
        if cls._instance is None:
            with cls._lock:
                if cls._instance is None:
                    cls._instance = cls()
        return cls._instance
```

### 全局可变状态
- 所有跨线程访问的模块级 dict / list / set 必须用 `threading.Lock` 或 `RLock` 保护。
- 不要依赖 GIL 保证线程安全。

---

## 7. 数据校验

### DataFrame
所有外部数据源返回的 DataFrame 必须经过校验：非空 + 必需列 + dtype。OHLCV 用 `validate_ohlcv_dataframe`，其余用 `validate_dataframe`，见 project-patterns §4。

### API 入参
所有 API 请求参数必须用 **Pydantic model** 校验，禁止用裸 `dict` 解析 request body。

### 金融数据完整性
- 负价格 / 负成交量 → 拒绝或 flag。
- 开盘期间 price = 0 → flag 为可疑。
- A 股日涨跌幅 > ±20% → flag review。

---

## 8. 类型注解

- 跨模块函数参数 **禁止** 用 `Dict[str, Any]`。用 `TypedDict` / `dataclass` / Pydantic `BaseModel`。函数内部中间处理可以用 `Dict[str, Any]`。
- 所有公开方法（无前置下划线）必须有完整 parameter + return 类型注解。
- 私有方法强烈推荐有类型注解。
- `Optional[T]` 返回值的 docstring 必须说明何时返回 `None`。

---

## 9. 数据源

详见 [data-source-priority.mdc](mdc:.cursor/rules/data-source-priority.mdc)。要点：Tushare 最高优先级；所有 fetcher 继承 `BaseFetcher`，不得自己实现 rate limit / retry；实时数据动态 TTL。

---

## 10. 异步代码

- I/O bound 操作优先 `asyncio` + `aiohttp` / `aiofiles`，不要在 async 上下文中开 `ThreadPoolExecutor`。
- 不要在已运行的 event loop 中调用 `asyncio.run()`。
- `ThreadPoolExecutor` 仅用于无法 await 的同步第三方库，通过 `loop.run_in_executor()` 调用。
