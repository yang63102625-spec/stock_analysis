---
description: 需求分析与架构设计规范 (Requirements Analysis & Design)
globs: *
alwaysApply: false
---

# 需求分析与设计规范 (Requirements Analysis & Design)

当用户提出新功能需求、架构调整或复杂问题修复时，AI 必须遵循以下分析与设计流程，**在编写代码前先明确方案**：

## 1. 需求理解与澄清 (Understand & Clarify)
- **核心目标**：明确用户“为什么要加这个功能”以及“最终想要什么效果”。
- **边界与限制**：识别潜在的性能瓶颈、API 速率限制（如 Tushare/东财接口限制）、并发问题或数据一致性问题。
- **主动提问**：如果需求模糊或存在逻辑漏洞，必须向用户提出简短、明确的选择题或确认题，而不是自行瞎猜。

## 2. 现状调研 (Codebase Exploration)
- **复用优先**：在设计新方案前，必须使用 `Grep` 或 `Glob` 搜索现有代码库，寻找可复用的工具函数、基类或类似逻辑（如现有的 `DataFetcherManager`、`NotificationService` 等）。
- **避免重复造轮子**：绝对不要引入与现有库功能重叠的新第三方库（例如，如果已经用了 `requests` 或 `httpx`，就不要再引入另一个 HTTP 客户端，除非有特殊理由）。

## 3. 方案设计与确认 (Propose & Confirm)
- **首选 plan mode**：复杂需求优先 `SwitchMode` 到 plan，让 `CreatePlan` 承载方案确认；不要在普通 agent 模式里手写"是否同意按此方案执行？"问句重复 plan 的工作。
- **架构对齐**：说明新代码将放在哪个目录（如 `src/core/`，`src/services/` 或 `api/`），遵循现有的模块化设计。
- **简单需求**：单文件 / 单点修改不必进 plan；用 `TodoWrite` 列出 2-4 步即可开工。

## 4. 风险评估 (Risk Assessment)
- 评估新功能是否会影响现有核心链路（如每日定时复盘任务、推送通知）。
- 考虑异常情况（如网络断开、数据源返回空数据、大模型 API 超时）的降级或兜底策略。